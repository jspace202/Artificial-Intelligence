To run aihw5.py,

Install necessary libraries:

pip install pandas
pip install numpy
pip install matplotlib
pip install tensorflow
pip install keras

lastly, edit the path of the input file "IMDB Dataset.csv" to match it's location on your local machine